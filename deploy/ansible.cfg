[defaults]
# the host key is added to known_hosts from DEPLOY_HOST_KEY before the run
host_key_checking = True
interpreter_python = auto_silent
roles_path = ./roles

[privilege_escalation]
become_method = sudo
